[pytest]
addopts = -q --maxfail=1
testpaths = tests
markers =
    contract: Marks tests that hit live external APIs (requires provider keys)
    slow: Marks pipeline/outage/benchmark suites that take longer to execute
